var ProviderBase = function() {};

ProviderBase.prototype = {
  // @TODO: Instead of returning geocode, take second parameter as return function, because javascript.
  geocode: function(searchString) {}
};